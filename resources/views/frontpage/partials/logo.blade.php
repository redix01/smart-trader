<img
  src="{{ asset('img/brand-logo.svg') }}"
  alt="{{ $alt ?? 'Fortis Marketpro' }}"
  class="{{ $class ?? '' }}"
  style="height: {{ $height ?? '55px' }}; width: auto; background: transparent; {{ $style ?? '' }}"
>
